export enum msgStatus {
  SENT = 'sent',
  TAKEN = 'taken',
  COMPLETED = 'completed',
  REJECTED = 'rejected',
}
export enum msgType {
  CLEAN = 'clean',
  MAINTENANCE = 'maintenance',
  EXAMINATION = 'examination',
  CREATE = 'create',
  OTHER = 'other',
}
export enum message_type {
  REQUEST = 'request',
  FEEDBACK = 'feedback',
}
